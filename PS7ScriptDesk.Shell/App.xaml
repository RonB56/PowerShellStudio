<Application x:Class="PS7ScriptDesk.Shell.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:shell="clr-namespace:PS7ScriptDesk.Shell"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

        <FontFamily x:Key="Theme.FontFamily">Segoe UI Variable Display</FontFamily>
        <FontFamily x:Key="Theme.MonoFontFamily">Consolas</FontFamily>

        <sys:Double x:Key="Theme.FontSize.Body">13</sys:Double>
        <sys:Double x:Key="Theme.FontSize.Caption">12</sys:Double>
        <sys:Double x:Key="Theme.FontSize.Section">18</sys:Double>
        <sys:Double x:Key="Theme.FontSize.Heading">24</sys:Double>

        <CornerRadius x:Key="Radius.Small">8</CornerRadius>
        <CornerRadius x:Key="Radius.Medium">12</CornerRadius>
        <CornerRadius x:Key="Radius.Large">16</CornerRadius>
        <CornerRadius x:Key="Radius.Pill">999</CornerRadius>

        <Thickness x:Key="Padding.Control">14,8</Thickness>
        <Thickness x:Key="Padding.DenseControl">12,6</Thickness>
        <Thickness x:Key="Padding.Card">14</Thickness>
        <Thickness x:Key="Padding.SubtleCard">10,8</Thickness>

        <DropShadowEffect x:Key="CardShadowEffect"
                          BlurRadius="18"
                          ShadowDepth="0"
                          Opacity="0.18"
                          Color="{DynamicResource Theme.Shadow.Color}" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Theme.App.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamily}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
            <Setter Property="shell:UiScaleBehavior.IsEnabled" Value="True" />
        </Style>


        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
        </Style>

        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="{DynamicResource Theme.ToolTip.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.ToolTip.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.ToolTip.Foreground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.ToolTip.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamily}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
        </Style>

        <Style x:Key="PowerShellCompletionListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="6,3" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Completion.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Completion.Foreground}" />
            <Setter Property="Opacity" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Completion.HoverBackground}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Completion.HoverForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Completion.HoverForeground}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Completion.SelectedBackground}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Completion.SelectedForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Completion.SelectedForeground}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Completion.DisabledForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Completion.DisabledForeground}" />
                                <Setter Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Section}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="MutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
        </Style>

        <Style x:Key="CaptionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="PanelCardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Strong}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Medium}" />
            <Setter Property="Effect" Value="{StaticResource CardShadowEffect}" />
        </Style>

        <Style x:Key="SubtlePanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
        </Style>

        <Style x:Key="IdePaneBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Effect" Value="{x:Null}" />
        </Style>

        <Style x:Key="IdeSectionHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="IdeSectionHeaderBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Padding" Value="0,0,0,6" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="IdeToolWindowPrimaryTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="IdeToolWindowLabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="IdeToolWindowMetaTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="IdeHeaderPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Padding" Value="10,8" />
        </Style>

        <Style x:Key="IdeToolWindowStyle" TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Theme.App.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamily}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
        </Style>

        <Style x:Key="IdeToolWindowHeaderStyle" TargetType="Border" BasedOn="{StaticResource IdeHeaderPanelStyle}">
            <Setter Property="Padding" Value="{StaticResource Padding.SubtleCard}" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="IdeToolWindowHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource IdeSectionHeaderTextStyle}">
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
        </Style>

        <Style x:Key="IdeToolWindowContentBorderStyle" TargetType="Border" BasedOn="{StaticResource IdePaneBorderStyle}">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
        </Style>

        <Style x:Key="IdeCompactEditorHeaderPanelStyle" TargetType="Border" BasedOn="{StaticResource IdeHeaderPanelStyle}">
            <Setter Property="Padding" Value="8,3" />
        </Style>

        <Style x:Key="IdeStatusStripBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="0,1,0,0" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Padding" Value="10,7" />
        </Style>

        <Style x:Key="IdeCompactEditorStatusStripBorderStyle" TargetType="Border" BasedOn="{StaticResource IdeStatusStripBorderStyle}">
            <Setter Property="Padding" Value="8,3" />
        </Style>

        <Style x:Key="EditorSurfaceBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Editor.Background}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Effect" Value="{x:Null}" />
        </Style>

        <Style x:Key="DiagnosticsPanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme.Status.Warning.Background}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Status.Warning.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Medium}" />
        </Style>

        <Style x:Key="DiagnosticsItemButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="0,0,0,6" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
        </Style>

        <Style x:Key="ScrollBarPageButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder"
                                Margin="1"
                                Background="{DynamicResource Theme.ScrollBar.Thumb}"
                                CornerRadius="6" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Theme.ScrollBar.ThumbHover}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Width" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Root" Background="Transparent">
                            <Border x:Name="TrackBorder"
                                    Margin="2"
                                    Background="{DynamicResource Theme.ScrollBar.Track}"
                                    CornerRadius="6" />
                            <Track x:Name="PART_Track" Focusable="False" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton x:Name="DecreaseButton"
                                                  Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="ScrollBar.PageUpCommand" />
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollBarThumbStyle}" />
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton x:Name="IncreaseButton"
                                                  Style="{StaticResource ScrollBarPageButtonStyle}"
                                                  Command="ScrollBar.PageDownCommand" />
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter TargetName="TrackBorder" Property="Height" Value="8" />
                                <Setter TargetName="TrackBorder" Property="VerticalAlignment" Value="Center" />
                                <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                                <Setter TargetName="DecreaseButton" Property="Command" Value="ScrollBar.PageLeftCommand" />
                                <Setter TargetName="IncreaseButton" Property="Command" Value="ScrollBar.PageRightCommand" />
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter TargetName="TrackBorder" Property="Width" Value="8" />
                                <Setter TargetName="TrackBorder" Property="HorizontalAlignment" Value="Center" />
                                <Setter TargetName="PART_Track" Property="Orientation" Value="Vertical" />
                                <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="True" />
                                <Setter TargetName="DecreaseButton" Property="Command" Value="ScrollBar.PageUpCommand" />
                                <Setter TargetName="IncreaseButton" Property="Command" Value="ScrollBar.PageDownCommand" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="12" />
                    <Setter Property="Width" Value="Auto" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Control.Border}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Control.Foreground}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="{StaticResource Padding.Control}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Small}"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Control.HoverBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Control.PressedBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Pressed}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Strong}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1.02"
                                                         Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1.02"
                                                         Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1"
                                                         Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1"
                                                         Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="{StaticResource Padding.Control}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Pill}"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Accent.Foreground}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Hover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1.02"
                                                         Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1.02"
                                                         Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1"
                                                         Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1"
                                                         Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
        </Style>

        <Style x:Key="IdeDialogButtonBaseStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource Theme.Control.Background}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Strong}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="MinWidth" Value="84" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Pressed}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Pressed}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.72" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeDialogSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource IdeDialogButtonBaseStyle}" />

        <Style x:Key="IdeDialogPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource IdeDialogButtonBaseStyle}">
            <Setter Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Accent.Foreground}" />
        </Style>

        <Style x:Key="IdeDialogTertiaryButtonStyle" TargetType="Button" BasedOn="{StaticResource IdeDialogButtonBaseStyle}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="MinWidth" Value="72" />
        </Style>

        <Style x:Key="IdeDialogPanelStyle" TargetType="Border" BasedOn="{StaticResource SubtlePanelBorderStyle}">
            <Setter Property="Padding" Value="12" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Effect" Value="{x:Null}" />
        </Style>

        <!-- Shared contract for product-owned secondary windows. Keep these styles
             semantic so utility dialogs can adopt the existing theme without
             duplicating local visual values. -->
        <Style x:Key="IdeSecondaryWindowStyle" TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Theme.App.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontFamily" Value="{StaticResource Theme.FontFamily}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Body}" />
        </Style>

        <Style x:Key="IdeDialogTitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Section}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="IdeDialogDescriptionStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,4,0,0" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="IdeDialogSectionStyle" TargetType="Border" BasedOn="{StaticResource IdeDialogPanelStyle}">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="{StaticResource Padding.Card}" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Medium}" />
        </Style>

        <Style x:Key="IdeDialogFieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,12,10" />
        </Style>

        <Style x:Key="IdeDialogFieldHelpStyle" TargetType="TextBlock" BasedOn="{StaticResource IdeDialogDescriptionStyle}">
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
        </Style>

        <Style x:Key="IdeDialogFooterStyle" TargetType="Border">
            <Setter Property="Margin" Value="0,12,0,0" />
            <Setter Property="Padding" Value="0,14,0,0" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="0,1,0,0" />
        </Style>

        <Style x:Key="IdeDialogResultPanelStyle" TargetType="Border" BasedOn="{StaticResource IdeDialogPanelStyle}">
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Padding" Value="{StaticResource Padding.SubtleCard}" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
        </Style>

        <Style x:Key="IdeBottomPaneTabToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="MinHeight" Value="28" />
            <Setter Property="Padding" Value="8,4,8,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True" />
                                <Border x:Name="SelectionUnderline"
                                        Height="2"
                                        VerticalAlignment="Bottom"
                                        Background="Transparent" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter TargetName="SelectionUnderline" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BottomPaneHeaderBadgeBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="CornerRadius" Value="{StaticResource Radius.Small}" />
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Tertiary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="StatusBarHelpToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="MinHeight" Value="22" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Small}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Tertiary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource Theme.Input.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Input.Foreground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Input.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Small}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource Theme.Input.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Input.Foreground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Input.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Border x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource Radius.Small}"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Margin="10,4,28,4"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      IsHitTestVisible="False" />
                                    <Path x:Name="DropDownGlyph"
                                          Width="8"
                                          Height="5"
                                          Margin="0,0,11,0"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Data="M 0,0 L 4,5 L 8,0 Z"
                                          Fill="{DynamicResource Theme.Text.Secondary}" />
                                </Grid>
                            </Border>

                            <ToggleButton Focusable="False"
                                          Background="Transparent"
                                          BorderBrush="Transparent"
                                          BorderThickness="0"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border Background="Transparent" />
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Background="{DynamicResource Theme.Surface.Primary}"
                                            BorderBrush="{DynamicResource Theme.Input.Border}"
                                            BorderThickness="1"
                                            CornerRadius="{StaticResource Radius.Small}">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.72" />
                                <Setter TargetName="DropDownGlyph" Property="Fill" Value="{DynamicResource Theme.Text.Secondary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Tertiary}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <Border x:Name="CheckBorder"
                                    Width="15"
                                    Height="15"
                                    Margin="0,1,0,0"
                                    Background="{DynamicResource Theme.Input.Background}"
                                    BorderBrush="{DynamicResource Theme.Input.Border}"
                                    BorderThickness="1"
                                    CornerRadius="3"
                                    SnapsToDevicePixels="True">
                                <Path x:Name="CheckGlyph"
                                      Width="9"
                                      Height="7"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0,3 L 3,6 L 9,0"
                                      Stroke="{DynamicResource Theme.Accent.Foreground}"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Square"
                                      StrokeEndLineCap="Square"
                                      Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="7,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                                <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Theme.Input.FocusBorder}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource Theme.Surface.Tertiary}" />
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                                <Setter TargetName="CheckGlyph" Property="Data" Value="M 1,3.5 L 8,3.5" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter TargetName="CheckBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                                <Setter TargetName="CheckBorder" Property="Opacity" Value="0.68" />
                                <Setter TargetName="CheckGlyph" Property="Opacity" Value="0.68" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0"
                                    Style="{StaticResource IdeSectionHeaderBorderStyle}">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  TextElement.FontWeight="SemiBold" />
                            </Border>
                            <ContentPresenter Grid.Row="1"
                                              Margin="{TemplateBinding Padding}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuPopupSeparatorStyle" TargetType="Separator">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1"
                                Margin="8,4"
                                Background="{DynamicResource Theme.Menu.Separator}"
                                SnapsToDevicePixels="True" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MenuPopupContextMenuStyle" TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource Theme.Menu.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Menu.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Small}">
                            <StackPanel IsItemsHost="True"
                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="Separator" BasedOn="{StaticResource MenuPopupSeparatorStyle}" />
            </Style.Resources>
        </Style>

        <Style TargetType="ContextMenu" BasedOn="{StaticResource MenuPopupContextMenuStyle}" />

        <Style TargetType="Menu">
            <Setter Property="Background" Value="{DynamicResource Theme.Menu.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="Padding" Value="6,4" />
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.Foreground}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border x:Name="RootBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource Radius.Small}" />
                            <Grid x:Name="TopLevelHeaderGrid" Margin="{TemplateBinding Padding}">
                                <ContentPresenter ContentSource="Header"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  RecognizesAccessKey="True" />
                            </Grid>
                            <Grid x:Name="PopupItemGrid"
                                  Visibility="Collapsed"
                                  Margin="{TemplateBinding Padding}"
                                  TextElement.Foreground="{TemplateBinding Foreground}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="18" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="14" />
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="CheckGlyph"
                                           Grid.Column="0"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Text="✓"
                                           Visibility="Collapsed" />
                                <ContentPresenter Grid.Column="1"
                                                  VerticalAlignment="Center"
                                                  ContentSource="Header"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  RecognizesAccessKey="True" />
                                <TextBlock x:Name="GestureText"
                                           Grid.Column="2"
                                           Margin="18,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           Opacity="0.85"
                                           Text="{TemplateBinding InputGestureText}" />
                                <Path x:Name="SubmenuArrow"
                                      Grid.Column="3"
                                      Margin="10,0,0,0"
                                      Width="8"
                                      Height="8"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Fill="{TemplateBinding Foreground}"
                                      Stretch="Uniform"
                                      Data="M 0 0 L 4 4 L 0 8 Z"
                                      Visibility="Collapsed" />
                            </Grid>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade">
                                <Border x:Name="PopupBorder"
                                        Margin="0,2,0,0"
                                        Background="{DynamicResource Theme.Menu.Background}"
                                        BorderBrush="{DynamicResource Theme.Menu.Border}"
                                        BorderThickness="1"
                                        CornerRadius="{StaticResource Radius.Small}">
                                    <StackPanel IsItemsHost="True"
                                                Margin="4"
                                                KeyboardNavigation.DirectionalNavigation="Cycle" />
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="TopLevelHeaderGrid" Property="Visibility" Value="Visible" />
                                <Setter TargetName="PopupItemGrid" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom" />
                            </Trigger>
                            <Trigger Property="Role" Value="TopLevelItem">
                                <Setter TargetName="TopLevelHeaderGrid" Property="Visibility" Value="Visible" />
                                <Setter TargetName="PopupItemGrid" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="TopLevelHeaderGrid" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="PopupItemGrid" Property="Visibility" Value="Visible" />
                                <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" />
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Right" />
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuItem">
                                <Setter TargetName="TopLevelHeaderGrid" Property="Visibility" Value="Collapsed" />
                                <Setter TargetName="PopupItemGrid" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.HoverForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.HoverForeground}" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Menu.HoverBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.HoverForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.HoverForeground}" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Menu.HoverBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckGlyph" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Menu.DisabledForeground}" />
                                <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.Menu.DisabledForeground}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ContextMenu" BasedOn="{StaticResource MenuPopupContextMenuStyle}" />
                <Style TargetType="Separator" BasedOn="{StaticResource MenuPopupSeparatorStyle}" />
            </Style.Resources>
        </Style>

        <Style TargetType="ToolBarTray">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>

        <Style TargetType="ToolBar">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4,2" />
        </Style>

        <Style x:Key="IdeToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Control.Foreground}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="Margin" Value="1,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource Radius.Small}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Control.HoverBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Control.PressedBackground}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Pressed}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
                                <Setter Property="Cursor" Value="Arrow" />
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.72" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeToolbarTooltipHostStyle" TargetType="Grid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="IsHitTestVisible" Value="True" />
            <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
            <Setter Property="ToolTipService.InitialShowDelay" Value="700" />
            <Setter Property="ToolTipService.ShowDuration" Value="12000" />
        </Style>

        <Style x:Key="IdeToolbarPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource IdeToolbarButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="IdeToolbarIconButtonStyle" TargetType="Button" BasedOn="{StaticResource IdeToolbarButtonStyle}">
            <Setter Property="Width" Value="30" />
            <Setter Property="Padding" Value="6,3" />
        </Style>

        <Style x:Key="IdeToolbarSeparatorStyle" TargetType="Separator">
            <Setter Property="Width" Value="1" />
            <Setter Property="Height" Value="22" />
            <Setter Property="Margin" Value="7,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Width="1"
                                Background="{DynamicResource Theme.Border.Strong}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeSplitterGripStyle" TargetType="GridSplitter">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="ShowsPreview" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Grid x:Name="Root"
                              Background="{TemplateBinding Background}">
                            <Border x:Name="GripLine"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Background="{DynamicResource Theme.Border.Subtle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeColumnSplitterStyle" TargetType="GridSplitter" BasedOn="{StaticResource IdeSplitterGripStyle}">
            <Setter Property="Width" Value="6" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="ResizeDirection" Value="Columns" />
            <Setter Property="ResizeBehavior" Value="PreviousAndNext" />
            <Setter Property="Cursor" Value="SizeWE" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Grid x:Name="Root"
                              Background="{TemplateBinding Background}">
                            <Border x:Name="GripLine"
                                    Width="1"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Stretch"
                                    Margin="0,8"
                                    Background="{DynamicResource Theme.Border.Subtle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeRowSplitterStyle" TargetType="GridSplitter" BasedOn="{StaticResource IdeSplitterGripStyle}">
            <Setter Property="Height" Value="6" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="ResizeDirection" Value="Rows" />
            <Setter Property="ResizeBehavior" Value="PreviousAndNext" />
            <Setter Property="Cursor" Value="SizeNS" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridSplitter">
                        <Grid x:Name="Root"
                              Background="{TemplateBinding Background}">
                            <Border x:Name="GripLine"
                                    Height="1"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center"
                                    Margin="8,0"
                                    Background="{DynamicResource Theme.Border.Subtle}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="GripLine" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Strong}" />
            <Setter Property="BorderThickness" Value="1,1,1,0" />
            <Setter Property="Padding" Value="6,2" />
        </Style>

        <Style TargetType="StatusBarItem">
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Padding" Value="1,0" />
        </Style>

        <Style x:Key="IdeStatusBarSeparatorStyle" TargetType="Separator">
            <Setter Property="Width" Value="1" />
            <Setter Property="Height" Value="14" />
            <Setter Property="Margin" Value="2,0,6,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Width="1"
                                Background="{DynamicResource Theme.Border.Subtle}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{StaticResource Radius.Small}">
                            <ContentPresenter ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Selection.Background}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style x:Key="IdeTabItemStyle" TargetType="TabItem">
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="0,0,2,0" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="Transparent"
                                BorderBrush="{DynamicResource Theme.Border.Subtle}"
                                BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter x:Name="ContentSite"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True" />
                                <Border x:Name="SelectionUnderline"
                                        Height="2"
                                        VerticalAlignment="Bottom"
                                        Background="Transparent" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter TargetName="SelectionUnderline" Property="Background" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IdeCompactEditorTabItemStyle" TargetType="TabItem" BasedOn="{StaticResource IdeTabItemStyle}">
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="0,0,1,0" />
            <Setter Property="MinHeight" Value="28" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{DynamicResource Theme.Surface.Secondary}"
                                BorderBrush="{DynamicResource Theme.Border.Subtle}"
                                BorderThickness="1"
                                CornerRadius="{StaticResource Radius.Small}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource Theme.Surface.Primary}" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource Theme.Accent.Primary}" />
                                <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource Theme.Surface.Hover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Secondary}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border.Subtle}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowBackground" Value="Transparent" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource Theme.Surface.Tertiary}" />
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource Theme.Surface.Tertiary}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,6" />
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Primary}" />
        </Style>

        <Style x:Key="IdeToolWindowDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource {x:Type DataGrid}}">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="RowHeight" Value="24" />
            <Setter Property="ColumnHeaderHeight" Value="26" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Theme.Border.Subtle}" />
        </Style>

        <Style x:Key="IdeToolWindowListBoxItemStyle" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>

        <Style x:Key="IdeEmptyPaneTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text.Secondary}" />
            <Setter Property="FontSize" Value="{StaticResource Theme.FontSize.Caption}" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="IsHitTestVisible" Value="False" />
        </Style>

        <Style x:Key="ContextHelpToolTipStyle" TargetType="ToolTip">
            <Setter Property="Background" Value="{DynamicResource Theme.ToolTip.Background}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme.ToolTip.Foreground}" />
            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.ToolTip.Foreground}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.ToolTip.Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="HasDropShadow" Value="True" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Border Width="6"
                                        HorizontalAlignment="Left"
                                        Background="{DynamicResource Theme.Accent.Primary}" />
                                <ContentPresenter Margin="16,12,12,12" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
